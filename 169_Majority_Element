class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        for num in nums:
            count = 0
            for evaluation in nums:
                if num == evaluation:
                    count = count + 1
                if count > (len(nums) / 2):
                    return num
